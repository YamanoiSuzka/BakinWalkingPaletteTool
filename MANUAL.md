# PixelRecolor 操作マニュアル

## 1. PixelRecolorとは

PixelRecolorは、PNG画像に使われている色を確認し、別の色へ置き換えるWindows用ツールです。

一般的なドット絵の単体編集に加えて、RPG Developer Bakin向け素材のような、同じキャラクターの複数アニメーション画像をまとめて色変更できます。

主な機能は次のとおりです。

- 単体PNG、またはフォルダー内にある複数PNGの読み込み
- PNGに使われている色とピクセル数の一覧表示
- パレットまたはプレビューからの色選択
- カラーピッカーによる1色ずつの置換
- 複数色の色相・彩度・明度・透明度の一括調整
- キャラクターグループ全体への共通色置換
- マウスホイールによるプレビューの拡大・縮小
- UNDO・REDO
- 単体PNGの別名保存
- キャラクターに属する全アニメーション画像の一括保存

## 2. 動作環境

- Windows 10またはWindows 11
- .NET 8 Desktop Runtime
- PNG画像

発行フォルダーから利用する場合は、`PixelRecolor.exe`だけを移動せず、同じフォルダーに発行された関連ファイルと一緒に保管してください。

## 3. 画面構成

### 左側：画像グループ一覧

読み込んだPNGが表示されます。

- Bakin形式の画像は、キャラクター名ごとのグループになります。
- 一般的な単体PNGは、名前の後ろに「（単体）」と表示されます。
- グループ名をクリックすると、所属するPNGの表示・非表示を切り替えられます。
- PNG名をクリックすると、その画像が編集対象になります。

### 右上：画像プレビュー

選択中のPNGを表示します。透明部分は市松模様で確認できます。

### 右下：カラーパレット

画像内で使用されている色を、使用ピクセル数の多い順に表示します。

各色には次の情報が表示されます。

- 色見本
- `#RRGGBBAA`形式のカラーコード
- 使用ピクセル数

カラーコード末尾の`AA`がアルファ値です。

- `00`：完全透明
- `FF`：完全不透明

完全透明になった色もパレットから削除されず、引き続き選択・調整できます。

## 4. PNGを読み込む

### フォルダー内のPNGをまとめて読み込む

1. 左上の「フォルダ選択」を押します。
2. PNGが入っているフォルダーを選択します。
3. 左側の画像グループ一覧から編集するPNGを選びます。

選択フォルダーの直下にあるPNGだけが対象です。サブフォルダー内のPNGは読み込みません。PNG以外のファイルは無視されます。

### PNGを1ファイルだけ読み込む

1. 左上の「PNG選択」を押します。
2. 編集するPNGを選択します。

他のファイルへ色変更を反映させたくない場合は、単体読み込みが便利です。

## 5. キャラクターグループの判定

ファイル名が次の形式なら、同じキャラクター名のPNGを一つのグループにまとめます。

```text
キャラクター名_アニメーション名.png
```

例：

```text
villager01_wait.png
villager01_walk.png
villager01_run.png
```

この場合は`villager01`グループとして扱われ、どのアニメーションで色を変更しても、同じ色が使われているグループ内の全画像へ反映されます。

区切りには、拡張子を除いたファイル名の最後の`_`を使用します。

```text
villager_red_wait.png
```

この例の解析結果：

```text
キャラクター名：villager_red
アニメーション名：wait
```

有効な区切りがないPNGは、1ファイルずつ独立した画像として扱われます。

> 注意：フォルダー読み込みでは、一般的なPNGでもファイル名に`_`があるとキャラクター形式として解析されます。同じ接頭名を持つ画像をまとめたくない場合は、PNGを単体で読み込んでください。

## 6. 1色を置き換える

色の置換は、パレットまたはプレビューから行えます。

### パレットから置き換える

1. 置き換えたい色を左クリックします。
2. カラーピッカーで変更後の色を選びます。
3. 「OK」を押します。

### プレビューから置き換える

1. プレビュー上の変更したいピクセルを左クリックします。
2. カラーピッカーで変更後の色を選びます。
3. 「OK」を押します。

カラーピッカーによる変更では、現在の透明度を維持したままRGBを変更します。透明度を変更する場合は「複数色の一括調整」を使用してください。

キャラクターグループの場合、選んだ色と完全に同じ色が使われている全アニメーションへ置換が反映されます。別グループや別キャラクターには影響しません。

## 7. 複数の色を選択する

### パレットから選択する

- 色を右クリック：選択
- 選択中の色をもう一度右クリック：選択解除

選択中の色は青色の枠で表示されます。

### プレビューから選択する

- ピクセルを右クリック：その色を選択
- 選択済みの色をもう一度右クリック：選択解除

### 全選択・全解除

「全選択 / 全解除」チェックボックスを使用します。

- チェックする：表示中のパレット色をすべて選択
- チェックを外す：選択をすべて解除

## 8. 複数色を一括調整する

1. 調整したい色を右クリックで選択します。
2. パレット下の「複数色の一括調整」を押します。
3. スライダーまたは数値入力でパラメータを設定します。
4. 「選択色に適用」を押します。
5. 作業を終える場合は「閉じる」を押します。

### 色相：0～180

色相を現在の色から移動します。値が大きいほど反対色に近づき、180で補色になります。

グレー、白、黒など彩度が0の色は、色相だけを変更しても見た目が変わりません。色味を付けたい場合は彩度も上げてください。

### 彩度：-100～100

- 正の値：色を鮮やかにする
- 負の値：グレーに近づける

既に最大または最小の色は、それ以上同じ方向へ変更できません。

### 明度：-100～100

- 正の値：明るくする
- 負の値：暗くする

既に白または黒の限界に達している場合は、それ以上同じ方向へ変更できません。

### 透明度：-100～100

- 正の値：透明にする
- 負の値：不透明にする

既に完全に不透明な色へ負の値を設定しても変化しません。完全透明な色へ正の値を設定した場合も同様です。

### パラメータをリセットする

「パラメータをリセット」を押すと、色相・彩度・明度・透明度を初期値の0へ戻します。すでに適用済みの画像変更を元に戻すボタンではありません。適用済みの変更はUNDOで戻してください。

### 色が変わらない場合

指定した値で色が変化しない場合は、理由を示すメッセージが表示されます。

代表例：

- 無彩色へ色相変更だけを適用した
- 彩度や明度がすでに上限・下限に達している
- 完全に不透明な色をさらに不透明にしようとした
- 調整量が小さく、8ビットカラーへ丸めた結果が元色と同じになった

## 9. プレビューを拡大・縮小する

プレビュー上でマウスホイールを回します。

- ホイール上：拡大
- ホイール下：縮小
- 最小倍率：25%
- 最大倍率：1600%

現在倍率はプレビュー右下に表示されます。カーソル位置を基準に拡大・縮小され、拡大後も左右クリックによる色操作が可能です。

色変更、UNDO、REDOでは現在倍率を維持します。別の画像を選ぶと100%へ戻ります。

## 10. UNDO・REDO

- 「UNDO」：直前の色変更を取り消す
- 「REDO」：取り消した色変更をやり直す
- `Ctrl+Z`：UNDO
- `Ctrl+Y`：REDO

履歴は画像グループごとに管理されます。別キャラクターの履歴には影響しません。

複数色の一括調整は、複数の色を変更していても1回の操作として記録されます。

## 11. 編集したPNGを保存する

元のPNGは編集操作だけでは変更されません。保存するときは必ず別名を指定します。

1. 保存したい画像またはキャラクターグループを選びます。
2. 画面上部の保存アイコン付き「別名保存する」を押します。
3. 新しい名前を入力します。
4. 出力フォルダーを指定します。
5. 「保存」を押します。

元と同じ名前は指定できません。保存先に同名ファイルがある場合は、上書き確認が表示されます。

### 単体PNGを保存する場合

新しい名前がそのままファイル名になります。

```text
slime.png
    ↓ 新しい名前：slime_red
slime_red.png
```

### キャラクターグループを保存する場合

キャラクター名の部分だけを変更し、アニメーション名を維持して全PNGを保存します。

```text
villager01_wait.png
villager01_walk.png
villager01_run.png
    ↓ 新しい名前：villager01_red
villager01_red_wait.png
villager01_red_walk.png
villager01_red_run.png
```

保存対象は選択中の1アニメーションだけではなく、そのキャラクターグループに属する全アニメーションです。

## 12. 注意点

### 色置換は完全一致です

置換対象はRGBA値が完全に一致するピクセルです。見た目が似ていても別のRGBA値なら別色として扱われます。

アンチエイリアス、半透明の境界、グラデーションを含む画像では色数が多くなります。必要な色を複数選択して一括調整してください。

### 透明色も編集対象です

完全透明なピクセルにもRGB値が保持されている場合があります。そのため、見た目が透明でも複数の透明色がパレットに表示されることがあります。

### 読み込み元を変更すると未保存の編集は消えます

別のフォルダーやPNGを読み込むと、現在の色置換内容とUNDO・REDO履歴は破棄されます。必要な画像は先に保存してください。

### アプリ終了時に編集状態は保存されません

現在はパレット設定や編集履歴のJSON保存には対応していません。アプリを終了する前に変換済みPNGを保存してください。

### サブフォルダーは検索しません

フォルダー読み込みの対象は、選択フォルダー直下のPNGだけです。

### PNG以外は扱いません

JPEG、BMP、GIFなどは読み込み対象外です。拡張子が`.png`でも、ファイル内容が壊れている場合は読み込みエラーになります。

### Windowsの警告について

署名されていない配布ファイルでは、Windows SmartScreenなどの警告が表示される場合があります。配布元を確認したうえで実行してください。

## 13. 困ったとき

### 「複数色の一括調整」が押せない

調整対象の色が選択されていません。パレットまたはプレビュー上の色を右クリックしてください。

### 別の画像まで色が変わった

同じキャラクターグループとして解析されています。ファイル名の最後の`_`より前が同じPNGには、共通の色置換が適用されます。

個別に編集したい場合は「PNG選択」から単体で読み込んでください。

### 保存した画像名にアニメーション名が付く

ファイル名が`名前_アニメーション.png`形式として解析されています。単体画像として名前全体を変更したい場合は、元ファイル名の`_`を避けるか、保存後に必要な名前へ変更してください。

### 色を完全透明にしたらプレビューで見えなくなった

正常な動作です。色自体はパレットに残っており、カラーコードの末尾が`00`になります。パレットから再選択して透明度を戻せます。

### 大きい画像のドットが見づらい

プレビュー上でマウスホイールを上へ回して拡大してください。倍率は最大1600%です。

