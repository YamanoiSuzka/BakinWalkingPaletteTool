<Window x:Class="BakinWalkingPaletteTool.SaveCharacterDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="画像を別名保存"
        Width="560"
        MinWidth="480"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock FontSize="18"
                   FontWeight="SemiBold"
                   Text="選択中の画像を別名保存" />
        <TextBlock Grid.Row="1"
                   Margin="0,6,0,16"
                   Foreground="#FF667085"
                   Text="グループ画像ではアニメーション名を維持し、指定した名前で一式を保存します。" />

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="130" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock VerticalAlignment="Center"
                       Text="新しい名前" />
            <TextBox x:Name="CharacterNameTextBox"
                     Grid.Column="1"
                     Padding="8,6"
                     TextChanged="Input_TextChanged" />
        </Grid>

        <Grid Grid.Row="3"
              Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="130" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock VerticalAlignment="Center"
                       Text="出力フォルダー" />
            <TextBox x:Name="OutputFolderTextBox"
                     Grid.Column="1"
                     Padding="8,6"
                     TextChanged="Input_TextChanged" />
            <Button Grid.Column="2"
                    Margin="8,0,0,0"
                    Click="BrowseButton_Click"
                    Content="参照..." />
        </Grid>

        <TextBlock x:Name="ValidationMessageTextBlock"
                   Grid.Row="4"
                   Margin="130,8,0,0"
                   Foreground="#FFB42318"
                   TextWrapping="Wrap" />

        <StackPanel Grid.Row="5"
                    Margin="0,16,0,0"
                    HorizontalAlignment="Right"
                    Orientation="Horizontal">
            <Button MinWidth="90"
                    IsCancel="True"
                    Content="キャンセル" />
            <Button x:Name="SaveButton"
                    Margin="8,0,0,0"
                    MinWidth="90"
                    IsDefault="True"
                    Click="SaveButton_Click"
                    Content="保存" />
        </StackPanel>
    </Grid>
</Window>
